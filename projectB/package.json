{
  "name": "project-b",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "pnpm install && pnpm build && node dist/index.js"
  },
  "dependencies": {
    "model": "file:../model"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
